# Open Questions

## Terminology and requirements
1. Следует ли в спецификациях везде использовать нормализованный термин `NEUTRAL`, считая `NETURAL` опечаткой из ТЗ?
2. Что именно означает требование `ROI > 300%`: прогнозная метрика, фильтр для открытия или целевая оценка сетки в симуляции?
3. Максимум уровней сетки зафиксирован как 7; нужно ли уже сейчас проектировать конфигурацию с потенциальным расширением до 10?

## Exchange and execution
4. Какие именно типы ордеров допустимы для live execution на Binance USDC futures в рамках grid-логики?
5. Как должен подтверждаться режим isolated margin и «ползунок маржи на максимум» перед каждым размещением?
6. Нужен ли лимит на количество одновременно активных сеток по инструменту и по всему портфелю?

## Decision pipeline
7. Нужны ли отдельные policy-пороги открытия для LONG, SHORT и NEUTRAL, или допускается общий threshold confidence/eligibility?
8. Какой минимальный набор обязательных полей должен быть у `feature snapshot`, чтобы probability engine имел право публиковать решение?
9. Как формально разделить случаи `neutral_only` и `blocked` в grid synthesis, чтобы downstream-модули трактовали их одинаково?

## Neutral grid specifics
10. Какой точный формат webhook/event требуется для снятия NEUTRAL grid?
11. Какие дополнительные условия аварийного снятия NEUTRAL grid должны срабатывать независимо от webhook?
12. Считается ли частичное закрытие NEUTRAL grid допустимым сценарием или нужна только полная отмена/остановка?

## Process and governance
13. Какие именно stage-based артефакты должны возвращать субагенты: markdown spec, schema, test plan, pseudocode?
14. Нужно ли поддерживать отдельный registry decision cycles помимо `memory/task_queue.md` и audit artifacts?
15. Как формально проверять, что внутренняя логика analysis layer остаётся воспроизводимой без жёсткой фиксации индикаторов/школ анализа?
